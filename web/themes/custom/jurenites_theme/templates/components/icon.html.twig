<icon class="icon{% if icon_class %} {{ icon_class }}{% endif %}{% if is_link_prefix %} icon--link-prefix{% endif %}" name="{{ icon_name }}" data-icon-name="{{ icon_name }}" aria-hidden="true">
  {% include '@jurenites_theme/components/icon-geometry.html.twig' with { icon_name: icon_name, with_active_state: is_link_prefix|default(false) } only %}
</icon>
