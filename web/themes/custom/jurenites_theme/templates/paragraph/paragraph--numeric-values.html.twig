{# Numeric Values section; child tiles own their number calculation and semantics. #}
<section{{ attributes.addClass('numeric-values').setAttribute('aria-label', 'Key figures'|t) }}>
  <ul class="numeric-values__grid">
    {{ content.field_numeric_items }}
  </ul>
</section>
