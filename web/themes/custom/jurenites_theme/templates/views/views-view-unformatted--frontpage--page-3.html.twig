{# Videos grid preserves the chronological order supplied by Views. #}
{% if title %}
  <h3>{{ title }}</h3>
{% endif %}
<div class="video-grid">
  {% for row in rows %}
    <div{{ row.attributes.addClass('video-grid__item') }}>
      {{- row.content -}}
    </div>
  {% endfor %}
</div>
