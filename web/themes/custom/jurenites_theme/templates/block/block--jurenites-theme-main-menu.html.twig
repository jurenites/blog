<nav class="site-header__navigation" aria-label="{{ configuration.label }}"{{ attributes|without('class', 'role', 'aria-label', 'aria-labelledby') }}>
  {{ title_prefix }}
  {{ content }}
  {{ title_suffix }}
</nav>
