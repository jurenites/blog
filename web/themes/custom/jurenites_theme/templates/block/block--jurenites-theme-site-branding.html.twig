{{ title_prefix }}
{% if site_logo %}
  <a class="site-header__brand" href="{{ path('<front>') }}" rel="home" aria-label="{{ 'Jurenites home'|t }}">
    <img class="site-header__logo{% if uses_lego_logo %} site-header__logo--lego{% endif %}" src="{{ site_logo }}" alt="" fetchpriority="high" />
  </a>
{% endif %}
{{ title_suffix }}
