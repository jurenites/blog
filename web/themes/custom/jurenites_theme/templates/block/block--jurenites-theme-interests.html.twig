{# Homepage placement for the reusable Interests Content Block. #}
<section{{ attributes.addClass('interest-tags') }} aria-label="{{ configuration.label }}">
  {{ title_prefix }}
  {{ title_suffix }}
  {{ content }}
</section>
