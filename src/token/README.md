# Token Source

`tokens.yaml` is the editable source of truth for design tokens.

Generated token artifacts live at:

- `../../generated/styles/_tokens.scss` for CSS/SCSS consumers
- `../../generated/token/tokens.js` for Storybook JS consumers

Both should be rebuilt from this YAML file instead of edited by hand.
